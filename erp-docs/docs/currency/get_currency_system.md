# `get_currency_system()`

La función `get_currency_system()` permite obtener la moneda que esta usando el usuario en el ERP.

Es útil para mostrar importes de forma consistente dentro del ERP.

---